# Coconut workshop for HaxeSummit2017

Setup:

```bash
# install
git clone https://github.com/MVCoconut/HaxeSummit2017 cocodemo && cd cocodemo && npm install
# run
npm start
```

And now: http://localhost:8080/